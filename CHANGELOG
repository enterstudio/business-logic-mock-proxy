## Changelog

### 0.0.2
* Add HTTP dashboard, installed through github and accessible through /interface
* Add helper REST routes for services (email, push, logging) to support HTTP dashboard
* Automatically create user collection with an index on 'username', to mirror production
* Add the ability to import collection data
* Add the ability to export collection data as a zip file
* Add coffeelint pretest
* Add/change/fix tests

### 0.0.1
* Create project, initial commits